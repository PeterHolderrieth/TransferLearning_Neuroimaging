Sex-pretrained: done - however, it is very weird that the results are only slighlty better than
the ones not trained at all. We still run two experiments where re-run with a very small learning rate to see how that goes.
We should also think re-training only the final layer to see how that goes.

Scratch, Elastic, direct transfer: RUNNING FINAL.
